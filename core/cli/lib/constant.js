const LOW_NODE_VERSION = '12.0.0'

const DEFAULT_HOME_CLI_CONFIG = '.imooc-cli'

const LOG_LEVEL_VERBOSE = 'verbose'

module.exports = {
  LOW_NODE_VERSION,
  DEFAULT_HOME_CLI_CONFIG,
  LOG_LEVEL_VERBOSE
}
